name: Mutation testing

# Thin caller of the shared mutation-testing reusable workflow; caller
# guide: leynos/shared-actions docs/mutation-mutmut-workflow.md.
on:
  schedule:
    - cron: "35 6 * * *"
  workflow_dispatch:

permissions: {}

concurrency:
  group: mutation-testing-${{ github.ref }}
  cancel-in-progress: false

jobs:
  mutation:
    permissions:
      contents: read
      id-token: write # OIDC workflow-source resolution
    uses: leynos/shared-actions/.github/workflows/mutation-mutmut.yml@8add2d99854a5b77548eae98cca59202e68fefc8
    with:
      # Flat layout: the mutable source lives in lading/, not src/.
      paths: "lading/"
      # No src/ prefix to strip when translating changed files to
      # module globs.
      module-prefix-strip: ""
